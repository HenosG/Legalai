import PublicPageTemplate from "@/components/public/PublicPageTemplate";

export default function CRMPlatform() {
  return (
    <PublicPageTemplate
      eyebrow="CRM"
      title="Keep every client relationship organized from first contact to final delivery."
      description="Manage leads, client records, notes, proposals, active projects, and history without losing context across tools."
    />
  );
}