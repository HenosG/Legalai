import PublicPageTemplate from "@/components/public/PublicPageTemplate";

export default function ClientPortalPlatform() {
  return (
    <PublicPageTemplate
      eyebrow="CLIENT PORTAL"
      title="Give clients a clear window into approved work."
      description="Create a branded client experience for project progress, proposals, invoices, documents, and updates."
    />
  );
}