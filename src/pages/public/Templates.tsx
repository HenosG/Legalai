import PublicPageTemplate from "@/components/public/PublicPageTemplate";

export default function Templates() {
  return (
    <PublicPageTemplate
      eyebrow="TEMPLATES"
      title="Start with proven structures, then make them yours."
      description="Explore practical templates for client discovery, proposals, scopes of work, onboarding, milestones, and updates."
    />
  );
}