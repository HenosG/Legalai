import PublicPageTemplate from "@/components/public/PublicPageTemplate";

export default function DevelopmentAgencies() {
  return (
    <PublicPageTemplate
      eyebrow="FOR DEVELOPMENT AGENCIES"
      title="Turn discovery into structured delivery."
      description="Move from a client brief to proposal, milestones, tasks, and delivery without rebuilding project context at every stage."
    />
  );
}